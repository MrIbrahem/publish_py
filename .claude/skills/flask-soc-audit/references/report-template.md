# SOC Audit Report Template

Copy this template and fill it in when generating `SOC_AUDIT_REPORT.md`.
Replace every `{{ ... }}` placeholder with actual content.

---

````markdown
# Separation of Concerns Audit Report

**Project**: {{ project name or root path }}
**Date**: {{ ISO date }}
**Auditor**: Claude (flask-soc-audit skill)
**Files Scanned**: {{ N }}

---

## Executive Summary

{{ 2–4 sentence overview: overall health, worst offender layers, top recommendation }}

### Finding Counts by Severity

| Severity    | Count       |
| ----------- | ----------- |
| 🔴 Critical | {{ N }}     |
| 🟠 High     | {{ N }}     |
| 🟡 Medium   | {{ N }}     |
| 🟢 Low      | {{ N }}     |
| **Total**   | **{{ N }}** |

### Most Problematic Files

| Rank | File         | Findings                       |
| ---- | ------------ | ------------------------------ |
| 1    | `{{ path }}` | {{ count }} ({{ severities }}) |
| 2    | `{{ path }}` | {{ count }} ({{ severities }}) |
| 3    | `{{ path }}` | {{ count }} ({{ severities }}) |

---

## Layer Health Overview

| Layer                                | Status                                   | Notes       |
| ------------------------------------ | ---------------------------------------- | ----------- |
| Routes (`public/`)               | {{ ✅ Clean / ⚠️ Issues / 🚨 Critical }} | {{ brief }} |
| Services (`db/services/`)            | {{ status }}                             | {{ brief }} |
| Models (`db/models/`)                | {{ status }}                             | {{ brief }} |
| Core (`core/`)                       | {{ status }}                             | {{ brief }} |
| Config (`config/`)                   | {{ status }}                             | {{ brief }} |
| Background Jobs (`background_jobs/`) | {{ status }}                             | {{ brief }} |
| API Services (`api_services/`)       | {{ status }}                             | {{ brief }} |
| Extensions (`extensions.py`)         | {{ status }}                             | {{ brief }} |

---

## Detailed Findings

<!-- Repeat this block for every finding, grouped by severity -->

### [{{ SEVERITY-EMOJI }} {{ SEVERITY }}] {{ ViolationCode }}: {{ Short title }}

**File**: `{{ relative/path/to/file.py }}`
**Line(s)**: {{ line range or "~L42" }}
**Violation**: {{ violation code from violation-patterns.md, e.g. V-R2 }}

**Problem**:
{{ 1–3 sentence explanation of why this is a SoC violation }}

**Offending Code**:

```python
{{ trimmed snippet, ≤15 lines }}
```
````

**How to Fix**:
{{ 2–4 sentence explanation of the correct approach }}

**Suggested Refactor**:

```python
{{ refactored snippet showing the fix }}
```

**Move to**: `{{ target layer / file path }}`

---

<!-- end of finding block -->

```

---

## Architectural Recommendations

<!-- Add 3–7 strategic recommendations, not tied to specific files -->

1. **{{ Title }}**: {{ explanation }}
2. ...

---

## Clean Files

The following files had **no violations** detected:

{{ bulleted list of paths }}

---

## Appendix: Import Dependency Graph

```

{{ ASCII or text representation of cross-layer imports found }}

```

*Generated by flask-soc-audit skill*
```
